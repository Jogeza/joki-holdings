# PUBLIC CLIENT CONFIGURATION (CRA exposes REACT_APP_* values to the browser)
# Required in production once /api/leads is deployed.
REACT_APP_LEAD_ENDPOINT=

# SERVER-ONLY SECRETS — never prefix these with REACT_APP_ and never commit values.
LEAD_STORAGE_PROVIDER=supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
LEAD_EMAIL_PROVIDER=resend
RESEND_API_KEY=
LEAD_NOTIFICATION_TO=
LEAD_NOTIFICATION_FROM=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
